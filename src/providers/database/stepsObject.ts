interface stepsObject{
  // stepParametersArray: Array<string>;
  // zeroOffset: Array<number>;
  // scalingFactor: Array<number>;
  // unitOfCurrent: string;
  unitOfCharge: string;
  offset: number;
  sensitivity: number;
  unitDependentOnSensitivity:string;
}
